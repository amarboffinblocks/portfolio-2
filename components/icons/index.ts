/**
 * App icon barrel — social (custom Lucide-compatible) + shared Lucide icons.
 * Prefer importing from `@/components/icons` in sections/features.
 * UI primitives under `components/ui` may keep direct `lucide-react` imports.
 */

export { Github, Instagram, Linkedin, WhatsApp, X } from './social'

export {
  ArrowDownToLine,
  ArrowUpRight,
  Bot,
  Cloud,
  Code2,
  Gauge,
  Mail,
  MapPin,
  Palette,
  Phone,
} from 'lucide-react'
